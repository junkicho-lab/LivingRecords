# 기여 가이드 (Contributing)

이 저장소는 **학습 프로젝트**다. 코드를 더하는 것만큼 *왜 그렇게 정했는지*를 남기는 것이 중요하다.
기여(코드·문서·번역·문제 제보)를 환영한다. 단, 이 프로젝트의 **작업 규율**을 따라달라.

> 기여하면 코드는 **MIT**, 문서는 **CC BY 4.0**(`LICENSE`·`LICENSE-docs.md`)으로 배포되는 데 동의하는 것으로 본다.

---

## 1. 기여의 종류

| 종류 | 예 | 어디 |
|---|---|---|
| **코드** | 새 슬라이스/기능, 버그 수정, 스파이크 | `LivingRecord/`, `spike/` |
| **문서** | 보고서·워크북·문제은행 개선, 오탈자 | `docs/` |
| **번역** | 한↔영 동기화 | `docs/en/` |
| **제보** | 버그·오류·개선 제안 | Issues |

---

## 2. 작업 규율 (이 프로젝트의 핵심 — 꼭 지킬 것)

1. **수직 슬라이스로.** 한 번에 한 흐름을 UI~저장까지 관통. PR도 슬라이스 크기로 작게.
2. **불확실하면 스파이크 먼저.** 새 기술·모델·알고리즘은 `spike/`에 작은 실험으로 검증하고 결과를 `docs/spike-findings.md`에 적은 뒤 본 구현.
3. **구현 → 실행 검증 → 커밋.** 빌드 통과 ≠ 동작. *실기기/실행으로 확인*하고 그 결과를 PR에 적는다. (음성·위젯·알림은 실기기)
4. **결정의 근거를 남긴다.** "왜 이렇게" 한 문단을 커밋 메시지/PR에. 대안·트레이드오프가 있으면 함께.
5. **막히면 범위를 좁히거나 되돌린다.** 통째로 시도하다 미완보다, 좁혀서 *작동하는* 것이 낫다.

---

## 3. 절대 규칙 (프라이버시·안전 — 예외 없음)

- **봉인(sealed) 포착·원문은 클라우드로 절대 보내지 않는다.** 클라우드로 가는 모든 경로에 `!sealed` 필터/증류층 변환이 있는지 확인. (참고: `Distillation`, `PeriodReview`)
- **비밀키·토큰을 코드에 하드코딩하지 않는다.** API 키는 Keychain/사용자 입력만.
- **인증·데이터 저장·클라우드 전송** 코드는 *사람이 직접 검토*한다고 가정하고 변경을 명확히 설명.
- **새 의존성(SPM 등)·큰 변경(스키마·폴더 구조)** 은 *먼저 이슈로 제안*하고 합의 후.
- **로그·URL·오류 메시지에 개인정보를 남기지 않는다.**

---

## 4. 커밋·PR

**브랜치:** `main`에 직접 push 금지. `feat/…`, `fix/…`, `docs/…` 브랜치에서.

**커밋 메시지:** 슬라이스/모듈 태그를 포함해 작게.
```
feat(S9): 냉각 능동화 — 식어가는 줄기 + 되살리기/접기
fix: 기록 탭 칩 바가 제목을 가리는 문제
docs: 워크북 3주차 빈칸 보강
```
> 본문에 *무엇을·왜·검증 결과*를 적는다.

**PR 체크리스트** (제출 전 자가 점검 — 채점 루브릭의 5역량과 같음):
- [ ] **A** 한 흐름이 *작동*한다(슬라이스 크기).
- [ ] **B** 빌드만이 아니라 *실행/스파이크로* 확인했고 결과를 적었다.
- [ ] **C** "왜 이렇게 정했나"를 한 문단 남겼다.
- [ ] **D** 봉인/원문이 새는 경로가 없는지 점검했다.
- [ ] **E** 막힘은 *좁혀/되돌려* 해결했고 그 과정을 적었다.
- [ ] 경고 0으로 빌드된다(`xcodebuild … build`).

---

## 5. 문서·번역 기여

- **교재 자료 수정 시** 원본을 고치고, 합본은 `docs/교재/build.sh`로 *다시 생성*(합본 `.md`는 생성물).
- **한↔영 동기화:** 한국어를 고치면 `docs/en/`의 대응 파일도 갱신. 용어는 `docs/en/README.md`·각 문서의 용어집을 따른다(예: 봉인=sealed, 줄기=throughline).
- **한국어 어미 교훈**(겠/해야/하자 등)은 영문에서도 *원문 + 글로스*로 유지 — 그게 교훈이다.
- **학생용 자료에 정답을 넣지 않는다.** 정답은 `강사용-모범답안집`·`채점-루브릭`·`문제은행`의 정답 부록(강사 전용)에만.

---

## 6. 개발 환경

- Xcode(iOS 26+ SDK), 빌드는 **실기기 권장**.
- 스파이크 실행: `swift spike/<파일>.swift`.
- 통합 교재 빌드: `docs/교재/build.sh [ko|en] [--student]`.

---

## 7. 행동 규범

서로 존중하고, *판단의 근거*로 토론한다. 실패·되돌림은 부끄러운 게 아니라 이 프로젝트의 가장 값진 교재다 — 솔직히 공유하라.

## 8. 라이선스 동의

- 코드 기여 → **MIT** (`LICENSE`).
- 문서/교재 기여 → **CC BY 4.0** (`LICENSE-docs.md`).
- 저작권자 표기를 특정 개인/기관으로 바꾸려면 메인테이너가 `LICENSE`를 수정한다.
