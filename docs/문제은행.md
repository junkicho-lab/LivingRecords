# 퀴즈·시험 문제은행 — "생동하는 기록" 프로젝트 강의

> 짝꿍: `8주-강의계획서.md`·`프로젝트-보고서.md`·`채점-루브릭.md`·`강사용-모범답안집.md`
> **정답·해설은 맨 끝 〈부록: 정답·해설〉(강사 전용)** — 학생 배부 시 그 절을 제거하라.
> 작성 2026-06-05

---

## 사용 안내

- **문항 ID** 예: `W3-MC2`(3주차 객관식 2번), `X-ES1`(통합 서술 1번). ID로 골라 시험지를 조립.
- **유형:** MC(객관식) · TF(참/거짓) · FB(빈칸) · SA(단답) · CODE(코드 읽기) · ES(서술/설계 판단).
- **난이도:** ★(기억) · ★★(이해·적용) · ★★★(분석·판단).
- **역량 태그:** A 슬라이스 · B 검증 · C 근거 · D 프라이버시 · E 실패(채점 루브릭과 연동).

---

# Part 1 · 주차별 문항

## 1주차 — 방법론·구상·검증

**W1-MC1 ★ [C]** 이 앱이 답하려는 "단 하나의 질문"은?
① 어떻게 더 빨리 메모할까 ② 무엇을 지속할 것인가 ③ 어떻게 요약을 잘할까 ④ 어떤 폰트가 좋을까

**W1-MC2 ★★ [B]** 스파이크(spike)의 목적으로 가장 적절한 것은?
① 재사용할 모듈을 미리 만든다 ② 디자인 시안을 고른다 ③ 불확실한 기술을 *버리는 실험*으로 검증한다 ④ 최종 UI를 확정한다

**W1-TF1 ★ [B]** (참/거짓) "빌드가 성공하면 그 기능은 완료된 것이다."

**W1-SA1 ★ [A]** UI~저장까지 한 흐름을 관통하는 최소 작업 단위를 무엇이라 하는가?

**W1-SA2 ★★ [B]** 스파이크와 프로토타입의 가장 큰 차이를 한 문장으로 쓰라.

**W1-FB1 ★ [C]** 작업 3박자: 구현 → ______ → 커밋.

---

## 2주차 — 포착·에너지·봉인

**W2-MC1 ★ [A]** SwiftData에서 모델 선언과 자동 조회에 쓰는 속성은?
① `@State`/`@Binding` ② `@Model`/`@Query` ③ `@Observable`/`@Environment` ④ `@Entity`/`@Fetch`

**W2-MC2 ★★ [D]** 녹음 후 오디오 원본을 *즉시 폐기*하고 에너지 점수만 남기는 주된 이유는?
① 배터리 절약 ② 가장 민감한 데이터를 안 쌓아 프라이버시 보호 ③ STT 정확도 향상 ④ 클라우드 업로드 속도

**W2-TF1 ★ [A]** (참/거짓) 포착 화면에는 주제를 고르는 분류 UI가 있다.

**W2-SA1 ★★ [C]** 에너지(각성도) 점수는 절대값이 아니라 무엇 대비 상대값으로 해석하는가?

**W2-CODE1 ★★ [A]** 다음 흐름의 빈칸을 채우라. `CaptureView`에서 텍스트 저장 시: `context.insert(c)` → `try? context.______()` → `Consolidator.______(c, ...)`.

---

## 3주차 — Obsidian 미러·분류·통합 ⭐

**W3-MC1 ★★ [B]** 한국어 임베딩에서 "중심화(centering)"가 필요한 이유는?
① 속도를 높이려고 ② 벡터가 한쪽에 몰리는 anisotropy를 보정하려고 ③ 봉인을 제외하려고 ④ 한글 인코딩 변환

**W3-MC2 ★★★ [B,C]** 임베딩 임계값 군집화가 *근본적으로* 실패한 이유로 가장 정확한 것은?
① 임베딩 차원이 너무 작아서 ② 합쳐야 할 쌍의 유사도가 합치면 안 될 쌍보다 낮아 *순위가 어긋나서* ③ 한국어를 지원하지 않아서 ④ 계산이 느려서

**W3-TF1 ★★ [E]** (참/거짓) 자동 분류는 "과병합"이 "과분할"보다 안전하다.

**W3-SA1 ★★ [C]** 자동 분류의 최종 설계 원칙을 6글자 이내로(예: "○○○○ + 큐레이션").

**W3-CODE1 ★★★ [C]** `ThemeAssigning.swift`는 주제 *번호(index)* 대신 `@Generable struct SameTopic { let same: Bool }`로 "같은 분야?"를 묻는다. 번호 방식을 버린 이유를 한 문장으로.

---

## 4주차 — 정리·클라우드·실기기

**W4-MC1 ★★ [D]** "2단계 증류"의 핵심은?
① 데이터를 두 번 압축한다 ② 원문은 로컬에 두고 *증류층(요약)만* 클라우드로 보낸다 ③ 클라우드에서 두 번 요약한다 ④ 봉인을 두 단계로 암호화한다

**W4-MC2 ★★ [D]** "클라우드는 의존이 아니라 ______다"에서 빈칸은?
① 필수 ② 백업 ③ 업그레이드 ④ 위험

**W4-TF1 ★ [B]** (참/거짓) 음성 받아쓰기는 시뮬레이터에서도 실기기와 동일하게 잘 된다.

**W4-SA1 ★★ [D]** 봉인 포착이 클라우드로 안 가는 것을 *코드의 어느 단계*에서 보장하는가?

**W4-FB1 ★ [B]** 실기기 실음성 문자 정확도(CER 기준) ≈ ______%.

---

## 5주차 — 지속 루프·결정적 게이트

**W5-MC1 ★ [A]** v2에서 새로 더해진 신호 셋이 아닌 것은?
① 실행(다짐) ② 냉각 ③ 연결 ④ 글꼴

**W5-MC2 ★★★ [B,C]** 스파이크 ⑦에서 의도 감지의 결론으로 옳은 것은?
① FM Bool이 가장 안정적이었다 ② 어미 게이트(10/10)가 FM(1~6/10)보다 안정적 → 판정은 게이트, FM은 라벨 보조 ③ 둘 다 못 쓴다 ④ 임베딩이 가장 정확했다

**W5-CODE1 ★★ [B]** `IntentionDetecting`의 `hasIntention`은 FM 없이도 동작한다. 그 이유는?

**W5-SA1 ★★ [C]** 모멘텀(떠오름/식어감)은 창(window)을 무엇과 무엇으로 갈라 비교하는가?

**W5-ES1 ★★★ [C]** (서술 5문장) "FM Bool 대신 한국어 어미 게이트를 택한" 판단을 *근거(측정값)·트레이드오프*를 들어 설명하라.

---

## 6주차 — 도달성·검색·부가

**W6-MC1 ★★ [A]** App Intent가 트리거 시 앱을 포그라운드로 띄우게 하는 속성은?
① `runsInBackground` ② `openAppWhenRun` ③ `launchOnTrigger` ④ `foregroundOnly`

**W6-MC2 ★★ [C]** 하이브리드 검색에서 *주력·신뢰*는 어느 쪽인가?
① 의미 검색 ② 전문(정확 포함) 검색 ③ 음성 검색 ④ 태그 검색

**W6-MC3 ★★ [D]** 백업에서 암호구절을 *바로* 키로 쓰지 않고 HKDF로 유도하는 이유로 적절치 않은 것은?
① 고정 길이의 강한 키 ② 랜덤 salt로 같은 암호도 매번 다른 키 ③ 사전공격 방어 ④ 파일 크기 축소

**W6-SA1 ★★ [A]** 의미 검색은 비용 때문에 매 타자가 아니라 언제 도는가?

**W6-CODE1 ★★★ [A]** 위젯의 버튼이 어떻게 *앱 안의* 녹음을 시작시키는지 `openAppWhenRun` 관점에서 설명하라.

---

## 7주차 — 생동하는 기록·LLM 위키

**W7-MC1 ★ [A]** 이 단계의 설계 전환을 한 단어 쌍으로?
① push→pull ② pull→push ③ local→cloud ④ text→voice

**W7-MC2 ★★★ [D,C]** 되새김(resurfacing)에서 14일+ 이전·봉인 제외로 후보를 고르는 *주된 이유*는?
① 저장 공간 ② 반추(rumination) 함정 방지 + 사생활 ③ 속도 ④ 클라우드 비용

**W7-SA1 ★★ [A]** "줄기(throughline)"를 한 문장으로 정의하라.

**W7-MC3 ★★ [D]** 위키 허브의 "한눈에" 요약은 어떻게 채워지는가?
① 항상 자동 로컬 ② 클라우드(Claude) 옵트인·명시적 갱신만 ③ 사용자가 직접 타이핑 ④ 채워지지 않는다

**W7-CODE1 ★★★ [A]** 반복 알림은 내용이 고정인데, 매일 *다른* 되새김을 어떻게 실었는가?

---

## 8주차 — 실패·점검에서 배우기

**W8-MC1 ★★★ [E,D]** 양방향 Obsidian의 "중복 생성" 사고의 *근본 원인*은?
① 네트워크 오류 ② 기존 미러 파일에 `id:`가 없어 가져오기가 전부 "새 포착"으로 만들어 복제 ③ 암호 분실 ④ 임베딩 실패

**W8-TF1 ★★ [E]** (참/거짓) "전체 다시 내보내기 먼저"라는 *안내문*만으로 중복 사고를 충분히 막을 수 있었다.

**W8-SA1 ★★ [B]** 임베딩 기반 주제 연결을 신뢰 못 한다는 걸 이 프로젝트에서 *몇 번* 반복 확인했는가? (그 세 지점을 함께 적으면 가점)

**W8-CODE1 ★★ [E]** `git show b0d28e6`(양방향 도입)에서 추가됐다 `f9fe264`(철회)에서 사라진 *핵심 파일*은?

**W8-ES1 ★★★ [D]** (서술) "봉인이 기기를 떠날 수 있는 모든 경로"를 3개 이상 들고, 각각 봉인이 제외되는지/어디서인지 적으라.

---

# Part 2 · 통합·서술형 (Cross-cutting)

**X-ES1 ★★★ [C,E]** 이 프로젝트에서 *되돌린/버린* 결정 1개를 골라(양방향 Obsidian, 임베딩 관련주제 제거, FM Bool 등) "무엇을·왜·트레이드오프·나라면"의 틀로 한 쪽 분량 분석하라.

**X-ES2 ★★★ [B]** "스파이크 우선" 규율이 이 앱의 *세 가지 이상*의 설계 결정을 어떻게 바꿨는지 사례를 들어 설명하라.(임베딩·STT·의도감지·검색 등)

**X-ES3 ★★★ [D]** 이 앱의 프라이버시 모델(로컬 우선·2단계 증류·봉인·전송 로그)을 설명하고, *남아 있는* 위험 1개와 강화안을 제시하라.

**X-ES4 ★★ [A]** "수직 슬라이스"로 작업할 때의 장점 2개와, 한 흐름을 *너무 넓게* 잡았을 때 생기는 문제 1개를 쓰라.

**X-SA1 ★★ [C]** "AI를 어디까지 믿을지"를 이 앱은 무엇으로 정했는가?(한 단어/구)

---

# Part 3 · 코드 읽기 심화

**C-CODE1 ★★★ [D]** 다음은 증류 코드의 일부다. 이 한 줄이 보장하는 *프라이버시 불변식*을 설명하라.
```swift
let caps = all.filter { !$0.sealed && $0.createdAt >= start && $0.createdAt < end }
```

**C-CODE2 ★★ [B]** 다음 의도 어미 게이트가 "환경에 안 흔들린다"고 말하는 근거를 코드 성질로 설명하라.
```swift
static let markers = ["겠", "해야", "하자", "봐야지", "려고", "시작하", "다짐", ...]
func hasIntention(_ text: String) -> Bool { Self.markers.contains { text.contains($0) } }
```

**C-CODE3 ★★★ [A]** 다음 되새김 선택 로직에서 "1순위"가 무엇인지, 그리고 `daySeed`가 하는 일을 설명하라.
```swift
// anniversaries: 같은 월·일의 옛 포착
if let pick = anniversaries.min(by: { $0.createdAt < $1.createdAt }) { return ... }
let daySeed = Int(today.timeIntervalSince1970 / 86_400)
let idx = ((daySeed % pool.count) + pool.count) % pool.count
```

---

# Part 4 · 시험 구성 예시 (Blueprint)

### 중간고사 (W1~W4, 100점, 50분)
- MC/TF 10문항 × 4점 = 40 (`W1-MC1·MC2`, `W2-MC1·MC2·TF1`, `W3-MC1·MC2·TF1`, `W4-MC1·TF1`)
- SA 4문항 × 6점 = 24 (`W1-SA1`, `W2-SA1`, `W3-SA1`, `W4-SA1`)
- CODE 2문항 × 8점 = 16 (`W2-CODE1`, `W3-CODE1`)
- ES 1문항 × 20점 = 20 (`X-ES4` 또는 `X-ES2`)

### 기말고사 (전체, 100점, 80분)
- MC/TF 12문항 × 3점 = 36 (각 주차 1~2개 골고루)
- SA 4문항 × 6점 = 24 (`W5-SA1`,`W6-SA1`,`W7-SA1`,`W8-SA1`)
- CODE 2문항 × 10점 = 20 (`C-CODE1`, `W7-CODE1`)
- ES 1문항 × 20점 = 20 (`X-ES1` 또는 `X-ES3`)

> **균형 팁:** 한 시험에 역량 A~E가 모두 1문항 이상 포함되게. ES는 *근거(C)·프라이버시(D)·실패(E)* 중심으로.

---

# 부록 · 정답·해설 〔강사 전용 — 학생 배부 시 삭제〕

### 1주차
- **W1-MC1** ② — "지속"이 차별점.
- **W1-MC2** ③ — 스파이크는 *버리는* 실험(사실만 얻고 폐기).
- **W1-TF1** 거짓 — 빌드 ≠ 동작. 실행·실기기 검증 필요.
- **W1-SA1** 수직 슬라이스(vertical slice).
- **W1-SA2** 예) "스파이크는 *버리는* 실험으로 불확실성만 없애고 폐기, 프로토타입은 발전시켜 쓸 수 있는 초안."
- **W1-FB1** (실행)검증.

### 2주차
- **W2-MC1** ② — `@Model`/`@Query`.
- **W2-MC2** ② — 가장 민감한 오디오를 안 쌓아 프라이버시(+용량).
- **W2-TF1** 거짓 — 분류 UI 없음(마찰 0).
- **W2-SA1** "평소(내 평소)" 대비 상대값.
- **W2-CODE1** `save` / `enqueue`.

### 3주차
- **W3-MC1** ② — anisotropy 보정.
- **W3-MC2** ② — 유사도 *순위*가 어긋남(예: 0.209 < 0.353) → 단일 임계값 불가.
- **W3-TF1** 거짓 — *과분할*이 과병합보다 안전(합치기로 되돌리기 쉬움).
- **W3-SA1** best-effort.
- **W3-CODE1** 예) "기기 FM이 주제 1개일 때 번호를 전부 0으로 *쏠려* 과병합 → Bool 판단으로 회피."

### 4주차
- **W4-MC1** ② · **W4-MC2** ③ · **W4-TF1** 거짓(시뮬레이터 STT 취약) · **W4-FB1** 92.8.
- **W4-SA1** `Distillation`(증류) 단계의 `!$0.sealed` 필터(+ `PeriodReview`에도 추가).

### 5주차
- **W5-MC1** ④ · **W5-MC2** ② .
- **W5-CODE1** "마커 substring 검사(순수 문자열) — FM 호출이 없어 결정적이고 환경에 안 흔들림. FM은 라벨(extractPhrase)에만, 실패 시 폴백."
- **W5-SA1** 전반(이전 절반) / 후반(최근 절반).
- **W5-ES1** 루브릭: 측정값(10/10 vs 1~6/10)·트레이드오프('좋겠다' 과발화 ↔ FM 불안정)·"나라면"이 있으면 우수↑.

### 6주차
- **W6-MC1** ② · **W6-MC2** ② · **W6-MC3** ④(파일 크기와 무관).
- **W6-SA1** 검색 실행(리턴) 시에만.
- **W6-CODE1** "`openAppWhenRun`이라 인텐트 perform이 *앱 프로세스*에서 실행됨 → 싱글턴(AppLaunchState) 신호를 세우면 SwiftUI가 관찰해 녹음 시작."

### 7주차
- **W7-MC1** ② · **W7-MC2** ② · **W7-MC3** ② .
- **W7-SA1** "여러 시기(서로 다른 주)를 가로질러 거듭 돌아온 주제 = 한결같이 마음 쓴 것."
- **W7-CODE1** "앞으로 7일치를 각각 *그날의 되새김* 내용으로 *비반복* 예약하고, 앱이 활성될 때마다 다시 채워 최신화."

### 8주차
- **W8-MC1** ② · **W8-TF1** 거짓(안내는 건너뛰기 쉽고 자동 동기화가 먼저 발화 → 안전장치는 *코드*여야).
- **W8-SA1** 3번 — 주제 군집(S4)·연결(S10)·위키 관련주제(7-3).
- **W8-CODE1** `ObsidianSync.swift`.
- **W8-ES1** 루브릭: 클라우드(✅ Distillation/PeriodReview)·Captures 미러(봉인은 `봉인/` 분리)·위키(✅)·*로컬 정리 md는 봉인 포함하나 클라우드엔 안 감*·볼트 iCloud 동기화 위험 — 클라우드 vs 로컬/볼트 구분 시 D 탁월.

### 통합·코드 심화
- **X-SA1** "측정(스파이크)으로" — 믿을 만큼만, 못 믿으면 결정적 규칙.
- **C-CODE1** "클라우드 입력을 만들 때 봉인 포착을 원천 제외 → 봉인은 절대 클라우드로 안 감."
- **C-CODE2** "FM 호출이 없는 순수 substring 검사 → 같은 입력에 항상 같은 결과(결정적). 기기·OS·실행마다 흔들리는 FM과 달리 일관."
- **C-CODE3** "1순위 = 같은 월·일의 'N년 전 오늘'. `daySeed`(일 단위 시드)로 *하루 동안 고정된* 인덱스를 만들어, 같은 날 다시 봐도 후보가 안 바뀌게."

---

> **출제 원칙(강사):** ★/★★/★★★를 7:2:1 정도로 섞고, 서술(ES)에서 *근거·트레이드오프*를 요구한다. 단순 암기보다 **"왜"** 를 묻는 문항을 늘릴수록 이 과목의 취지에 맞는다.
