# CLAUDE.md — 생동하는 기록 (Living Record)

> 이 파일은 **매 턴 항상 로드**된다. 짧게 유지하고, 상세는 `docs/`를 *가리키기만* 한다(`@import` 금지 — 일반 경로로 지연 로딩).
> 이 저장소는 **둘**이다: 🍎 iOS 앱(`LivingRecord/`·`spike/`) + 📚 8주 PBL 교재(`docs/`). 작업 전 어느 쪽인지 분간한다.

## 정체성
말로 쏟은 생각을 로컬에 모아 AI가 일/주/기간 회고로 가공하고, **"나는 무엇을 지속할 것인가"** 를 사람이 결정하도록 *증거*(반복·에너지·진화·냉각·연결·실행)를 보여주는 iOS 26+ 앱. **AI는 판결하지 않는다.**

## 🟦 성역 (절대 불변 · 예외 없음 · 변경 전 반드시 사용자 승인)
1. **봉인(sealed)·원문은 클라우드로 절대 안 나간다.** 클라우드로 가는 *모든* 경로에 `!sealed` 필터 / 증류층(요약) 변환이 있는지 확인(참고: `DeepSynthesizer`, `PeriodReview`, `ObsidianMirroring`).
2. **로컬 우선.** 원문·음성·에너지는 기기를 떠나지 않는다. 클라우드는 *옵트인* + *증류층만*. 클라우드를 한 번도 안 켜도 앱은 쓸모 있어야 한다.
3. **포착 = 마찰 0.** 말하거나 쓰면 끝. 분류·제목·태그를 묻는 UI를 포착 흐름에 추가하지 않는다.
4. **결정은 사람.** AI는 증거 제시까지. 지속/보류/접기를 AI가 자동 판결하지 않는다.
5. **비밀키·토큰 하드코딩 금지**(Keychain/사용자 입력만). 로그·URL·오류 메시지에 개인정보 금지.

## 스택 & 컨벤션
- SwiftUI · SwiftData · SpeechTranscriber(STT) · Foundation Models **+ MLX 폴백**(가드레일 회피) · NLContextualEmbedding **+ 중심화 필수**(anisotropy 보정) · vDSP(prosody) · Swift Charts · CryptoKit · App Intents/WidgetKit/UserNotifications · Claude API(옵트인 깊은 종합).
- 언어 한국어. **빌드 경고 0**(실기기 권장 — 음성·위젯·알림 제약). `main` 직접 push 금지 → `feat/`·`fix/`·`docs/` 브랜치.
- 경계 추상화: `Transcriber`/`LocalSynthesizer`/`ProsodyAnalyzer`/`Embedder`/`DeepSynthesizer` 인터페이스로 컴포넌트 교체 가능(`Interfaces.swift`).

## 작업 방식 — 발굴식 바이브 코딩 (이 프로젝트는 이미 체화함)
오래 살 코드 / 복잡한 도메인이면 따른다. 일회용·한 줄 수정·탐색 스파이크엔 적용하지 않는다(과적용 금지).
- **수직 슬라이스로** 한 흐름을 UI~저장까지 관통. PR도 슬라이스 크기로 작게.
- **불확실하면 스파이크 먼저** — `spike/`에 작은 실험으로 검증 → 결과를 `docs/spike-findings.md`에 적은 뒤 본 구현.
- **구현 → 실행 검증 → 커밋.** 빌드 통과 ≠ 동작. *실기기/실행*으로 확인하고 결과를 커밋/PR에 적는다.
- **한 번에 하나** + 결정의 *왜*를 남긴다. 막히면 범위를 좁히거나 되돌린다.
- **새 의존성(SPM)·스키마/폴더 변경 = 먼저 이슈로 제안**하고 합의 후. 추측 코딩 금지 — 모르면 *먼저 묻는다.*

▶ **새 기능·모듈·API·스키마를 *시작할 때*:** `~/Documents/vibes/excavation-템플릿/docs/excavation-vibe-coding.md` 전문(10단계 + 복사용 프롬프트)을 읽고 따른다.

## 코퍼스 (복리 자산 — 이미 존재. 새로 만들지 말고 여기에 적립)
- 결정과 *이유*: `docs/concept.md`(설계 토론·결정/미결) · `docs/v1~v4-slices.md`(슬라이스 계획·진행)
- 함정·교훈(스파이크 검증 ①~⑧): `docs/spike-findings.md` — 특히:
  - ⚠️ **양방향 Obsidian은 철회됨**(중복 사고). 단방향(앱→볼트)만. `Capture.mirrored`는 마이그레이션 회피용 잔존 필드 — **되살리지 말 것.**
  - ⚠️ **온디바이스 FM은 짧은 한국어 자동분류·의도감지에 불안정**(환경마다 다름). 결정적으로 가능한 건 결정적으로(예: 의도 어미 게이트 10/10), **FM은 보조(best-effort), 단독 의존 금지.**
  - ⚠️ **의미 검색은 보조·'추정'**(임계값 ~0.30). 전문 검색이 주력·신뢰. 하이브리드 유지.

## 하지 말 것
성역 변경 · 포착에 분류 UI 추가 · 봉인/원문 클라우드 누출 · 양방향 Obsidian 부활 · FM 단독 의존 · 실행 검증 없는 머지 · `main` 직접 push.
